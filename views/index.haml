- @databases.each do |db|
  %a{:href => "/" + db.name}
    %h2
      = db.name
  %ul
    - db.collections.each do |collection|
      %a{:href => "/" + File.join(db.name, collection)}
        %li #{collection}
